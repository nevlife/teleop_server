# Public DNS name or public IP advertised to rover/client by signaling.
TELEOP_PUBLIC_HOST=teleop.example.com

# Shared TURN credential. This is not an application login.
TELEOP_TURN_PASSWORD=change-this-before-internet-testing

# The official coturn image discovers the public IPv4 address at startup.
TELEOP_DETECT_EXTERNAL_IP=yes
